[project]
name = "big-iron-demo"
version = "0.1.0"
description = "Demo codebase for Big Iron — AI-Native SDLC"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
